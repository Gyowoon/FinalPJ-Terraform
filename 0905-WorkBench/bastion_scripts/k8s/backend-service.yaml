apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: shop
  labels:
    app: backend
    component: api
  annotations:
spec:
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
  sessionAffinity: None
